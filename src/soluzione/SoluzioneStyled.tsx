import styled from "styled-components";

const TestoSoluzione = styled.div`
  font-size: 28px;
`;

const ContainerSoluzione = styled.div`
  display: flex;
  flex-direction: row;
  padding: 2rem;
  gap: 2rem;
`;
export { TestoSoluzione, ContainerSoluzione };
